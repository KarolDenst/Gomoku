namespace MCST;

// This is not an Enum in case we want to do some bit operation magic later.
public static class Tiles
{
    public const int Empty = 0;
    public const int White = 1;
    public const int Black = -1;
}